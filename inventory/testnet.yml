---
all:
  vars:
    env: testnet
  children:
    bera:
      hosts:
        validator.bera.testnet.encapsulate.xyz:
          type: validator
        fullnode.bera.testnet.encapsulate.xyz:
          type: fullnode
